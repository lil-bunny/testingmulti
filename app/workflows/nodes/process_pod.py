# from app.tools.pod import process_pod_payload


# def process_pod(state):
#     result = process_pod_payload(state.data)
#     state.data["pod_processing"] = result
#     state.data["pod_exists"] = result["success"]
#     return state
